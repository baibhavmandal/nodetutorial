local dependency - use package for a paticular project

npm install <package name> || npm i <package name>

global dependency - install package for a paticular project

npm install -g <package name>

package.json

npm init (for step by step, press enter to skip)
npm init -y (everything by default)
we can also make it manually

